{
  "name": "create-opsgenie-alert-action",
  "version": "1.1.0",
  "main": "index.js",
  "repository": "git@github.com:rockem/create-opsgenie-alert-action.git",
  "author": "Eli Segal <eli.segal@gmail.com>",
  "license": "MIT",
  "scripts": {
    "compile": "NODE_OPTIONS=--openssl-legacy-provider ncc build index.js --license licenses.txt",
    "lint": "eslint . --fix --ext .js",
    "preversion": "rm -Rf node_modules && yarn --prod && yarn compile"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "@actions/core": "^1.6.0",
    "opsgenie-sdk": "^0.5.0"
  },
  "devDependencies": {
    "chai": "^4.3.6",
    "eslint": "^8.7.0"
  }
}
